<div class="scrollable">
<div class="scrollable-content">

  <!-- Test Buttons -->
  <div bs-panel title="Test it!">
    <div class="btn-group justified">
      <div class="btn btn-default" ng-click="logGlobalModel()">Log Config</div>
      <div class="btn btn-default" ng-click="findMatches(0)">Test Request</div> 
    </div>
  </div>

  <!-- Identity -->
  <div bs-panel title="Identity">
    <h3><small>Phone: {{config.model.identity.phone}}</small></h3>
    <h3><small>MD5: {{config.model.identity.md5}}</small></h3>
  </div>

  <!-- Invitees -->
<<<<<<< HEAD
  <div bs-panel title="Contacts">
    <!--
    <input type="text" name="newInvitee" ng-model="newInvitee" class="justified">
    <div class="btn btn-default justified" ng-click="mampfConfig.addInvitee(newInvitee)">Add Invitee</div>
    -->

    <li ng-repeat="contact in config.model.contacts">
      <h3><small>{{contact.name}} / {{contact.phoneNumbers[0]}} 
      <div class="btn btn-default btn-xs" ng-click="config.remContact(contact)">delete!</div></small></h3>
=======
  <div bs-panel title="Invitees">
    <li ng-repeat="invitee in config.model.requests[0].invitees">
      <h3><small>{{invitee.name}} / {{invitee.phone}} 
      <div class="btn btn-default btn-xs" ng-click="config.toggleInvitee(contact)">toggle!</div></small></h3>
>>>>>>> e1824261d4dd326c6850b4e302b646aa23a06388
    </li>
  </div>

  <!-- Location -->
  <div bs-panel title="Position">
    <h3><small>
      Lat: {{config.model.requests[0].currentPosition.latitude}}<br>
      Long: {{config.model.requests[0].currentPosition.longitude}}
    </small></h3>
  </div>

  <!-- Timeslots -->
  <div bs-panel title="Timeslots">
    <li ng-repeat="timeslot in config.model.requests[0].timeslots track by $index">
      <h3><small>Start: {{timeslot.startTime}}<br>End: {{timeslot.endTime}}
      <div class="btn btn-default btn-xs" ng-click="config.remTimeslot(timeslot)">delete!</div></small></h3>
    </li>
  </div>

  <!-- Overlay for response -->
  <div overlay="responseOverlay">
    <h4 class="overlay-title">Response</h4>    
    <div><div>Just the first: {{response.names[0]}}</div></div>
    <!-- ng repeat not working..-->
    <div ng-repeat="name in response.names">ASDF</div>

    <br>
    <p>{{response.full}}</p>

    <p toggle="off" bubble target="responseOverlay">
      <span class="btn btn-primary">Ok</span>
      <span class="btn btn-default">Cancel</span>
    </p>  
  </div>

</div>
</div>